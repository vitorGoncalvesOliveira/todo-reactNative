import { StyleSheet } from 'react-native';

export const styles =  StyleSheet.create({
    constainer:{
        height: 200,
        alignItems: 'center',
        backgroundColor: '#0D0D0D',
        padding: 24,
        marginTop: 20,
    
    },
    image: {
        marginTop:30,
    }

})