import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    emptyContainer: {        
        alignItems: 'center',
        width: '80%',
        justifyContent: 'center',
        marginLeft: 45,
        paddingTop:80,
        borderTopWidth: 1,
        borderColor: '#808080'        
    },
    emptyText: {
        color: '#808080',
        fontSize: 16
    },
    emptyTextTitle: {
        marginTop: 20,
        color: '#808080',
        fontWeight: 'bold',
        fontSize: 16
    },
})